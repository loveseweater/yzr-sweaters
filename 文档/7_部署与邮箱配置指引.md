# YZR Sweaters 官网部署与企业邮箱配置指引

本指引覆盖三件事：**域名托管到 Cloudflare、部署落地页到 Cloudflare Pages、注册企业邮箱**。按顺序执行即可。

> 你需要先在 [Cloudflare](https://dash.cloudflare.com/sign-up) 注册一个免费账号（用你的常用邮箱），后面所有操作都在它的控制台完成。

---

## 第一部分：把域名托管到 Cloudflare

你的域名 `yzrsweaters.com` 目前注册在**腾讯云**。托管到 Cloudflare 后，DNS（域名解析）改由 Cloudflare 统一管理，之后的邮箱记录、网站记录都在同一个地方加，方便管理。

1. 登录 Cloudflare 控制台，点 **Add a site**（添加站点）。
2. 输入 `yzrsweaters.com`，点 Continue。
3. 选 **Free（免费）** 计划，继续。
4. Cloudflare 会给你**两个 Nameserver（域名服务器）**，形如：
   ```
   xxx.ns.cloudflare.com
   yyy.ns.cloudflare.com
   ```
5. 回到**腾讯云域名控制台**，找到 `yzrsweaters.com` -> **修改 DNS 服务器**。
6. 把腾讯云默认的 nameserver 替换成 Cloudflare 给的那两个，保存。
7. 回到 Cloudflare，点 **Check nameservers** 或等 5 分钟到 24 小时（通常是几分钟到几小时）完成切换。

> 切换完成后，Cloudflare 里会出现你的域名，还能看到它自带的网站在线。这一步不会影响你现有的任何东西，因为新站点还没上线。

---

## 第二部分：把落地页部署到 Cloudflare Pages

你的落地页是纯静态网站（`index.html` + `images/` + `404.html`），Cloudflare Pages 有免费额度，正好托管它。

### 方式 A（推荐）：把文件夹拖到控制台（无需 GitHub）

1. 进入 Cloudflare 控制台 -> **Workers & Pages**（左侧）-> **Create** -> 选 **Pages**。
2. 选 **Direct Upload**（直接上传）。
3. 项目名填 `yzr-sweaters`，然后 **Upload assets**。
4. 上传你本地的 `G:\来发信开发客户\landing-page` 文件夹：
   - 把 `index.html`、`404.html`、`images/` 整个文件夹拖进去。
   - 点 **Deploy**（部署）。
5. 部署成功后，你会得到一个临时域名，例如 `yzr-sweaters.pages.dev`。**先用它测试页面**。

### 方式 B（推荐进阶）：连接 GitHub 自动部署

如果你会把代码放到 GitHub，之后每次 `git push` 都能自动更新网站，适合长期维护。

1. 把 `landing-page` 文件夹推到一个 GitHub 仓库。
2. 在 Cloudflare Pages 选 **Connect to Git**，选择该仓库。
3. 构建配置留空（纯静态，无构建命令），Direct 目录填 `/`（根目录）。
4. 保存后 Pages 会自动构建并部署。

### 绑定你自己的域名

1. 在 Pages 项目里点 **Custom domains** -> **Set up a custom domain**。
2. 输入 `yzrsweaters.com`，点 Continue。
3. 因为域名已经在 Cloudflare，它会自动添加一条 CNAME 记录并亮绿勾。
4. 稍等几分钟，访问 `https://yzrsweaters.com` 就能看到你的站点了。
5. 在站点设置里把 **SSL/TLS** 改为 **Full (strict)**，确保 HTTPS 正常；Cloudflare 会自动发免费证书。

---

## 第三部分：用 Cloudflare 免费版注册企业邮箱

> **重要提醒**：Cloudflare 官方免费版邮箱（Cloudflare Email Routing）只提供**转发**，不提供收件/发件服务——也就是你能把 `sales@yzrsweaters.com` 收到的信转发到你已有的私人邮箱（比如 QQ、163、Gmail），但**用这个域名发信**需要另外配一个发送服务。

如果你只是想**收**开发信/询盘转发到一个邮箱，用下面「方案 A」最快；如果你需要能**用 `sales@yzrsweaters.com` 直接发开发信**（外贸核心需求），用「方案 B」配第三方企业邮箱。

### 方案 A：Cloudflare Email Routing（免费，仅收件转发）

1. 在 Cloudflare 控制台打开域名 `yzrsweaters.com`。
2. 左侧 **Email** -> **Email Routing** -> **Get started**。
3. 自定义地址（Destination address）填你的**真实邮箱**（如 `xxxx@qq.com`）。
4. 点 **Create address**，填入 `sales`，这样 `sales@yzrsweaters.com` 的信会转发到你的真实邮箱。
5. 按提示给邮箱发一封确认信，点邮件里的确认链接。
6. 系统提示你添加一条 MX 记录和 SPF 记录，点 **Add records and enable** 自动完成。

> 这样，客户发到 `sales@yzrsweaters.com` 的信，都会转到你的私人邮箱，你能收到询盘。但这套**不能主动发信**。

### 方案 B：第三方企业邮箱 + SPF/DKIM（推荐，能收发开发信）

外贸需要「用域名邮箱主动发开发信」，建议用腾讯企业邮箱（腾讯云同源，你已熟悉）或阿里企业邮箱，再把它和 Cloudflare 的 DNS 结合。两种组合任选，**最终都要在 Cloudflare 里加记录**：

1. 到腾讯企业邮箱官网（`exmail.qq.com`）注册一个**免费版**企业邮箱，用域名 `yzrsweaters.com`。
2. 它会要求你验证域名——通常让你在 DNS 里加一条 TXT 记录（验证用）和一条 MX 记录。
3. 把腾讯给你的这些记录**加在 Cloudflare** 的 `yzrsweaters.com` DNS 里（DNS 记录 -> Add record）。常见的几条：
   - **MX 记录**：指向腾讯企业邮箱服务器（如 `mx1.exmail.qq.com` 优先级 5）
   - **TXT 记录**：`v=spf1 include:spf.exmail.qq.com ~all`（SPF，防止被当垃圾邮件）
   - **TXT 记录**：腾讯给的 DKIM 记录（用于邮件签名）
   - **CNAME 记录**：用于邮箱激活/网页登录
4. 验证通过后，你就能在腾讯企业邮箱后台创建账号，例如 `sales@yzrsweaters.com`，然后用它收信、发开发信了。

> **一句话原则**：无论用哪家邮箱，只要你的 DNS 在 Cloudflare，所有 MX / SPF / DKIM / TXT 记录都**在 Cloudflare 里加**，不要在腾讯云加。这样统一管理，避免两处不同步。

---

## 第四部分：把落地页的联系信息改成你的真实资料

网站上目前有一些占位信息，部署前请替换成真实资料（在 `landing-page/index.html` 里改）：

| 位置 | 占位内容 | 改成 |
|------|---------|------|
| 联系区手机 | `WhatsApp: +86 138 · Your Number` | 你的真实 WhatsApp/电话 |
| 销售邮箱 | `sales@yzrsweaters.com` | 你注册好的企业邮箱 |
| 表单处理 | `mailto:sales@yzrsweaters.com` | 已内置（免后端） |
| 页脚公司名 | 已写好 | `/` |

**表单处理**：网站表单已经内置了免后端的 `mailto` 方案——访客填写后点「Send Inquiry」，会自动打开他们的邮件客户端，把内容作为一封草稿发到你的 `sales@yzrsweaters.com`。**无需任何后端或第三方服务**，部署后即可用。前提是 `sales@yzrsweaters.com` 这个邮箱真实存在（见第三部分）。

---

## 第五部分：验证清单（部署后必查）

- [ ] 访问 `https://yzrsweaters.com` 正常显示页面，图片不裂。
- [ ] HTTPS 正常（浏览器地址栏有小锁），证书有效。
- [ ] 手机端打开正常（菜单可展开，图片不变形）。
- [ ] 给 `sales@yzrsweaters.com` 发一封测试信，确认能收到（Email Routing 或企业邮箱都测）。
- [ ] 用 `sales@yzrsweaters.com` 对外发一封测试信，确认对方能收到且**不进垃圾箱**（SPF/DKIM 配好）。
- [ ] 打开开发信里的官网链接 `https://yzrsweaters.com`，确认能访问——这会增加对方信任感。

---

## 域名解析速查表

| 记录类型 | 主机 | 值 | 用途 |
|---------|------|----|----|
| CNAME | `www` | `yzr-sweaters.pages.dev` | 网站（www 访问） |
| CNAME | `@`（根） | `yzr-sweaters.pages.dev` | 网站（裸域访问） |
| MX | `@` | 你的邮箱服务商 MX | 收企业邮件 |
| TXT | `@` | `v=spf1 include:... ~all` | 防垃圾（发信） |
| TXT | 各子域 | DKIM 签名 | 邮件防伪 |

> 不同邮箱服务商给的记录值不同，以你自己后台提示的为准。加完记录后 Cloudflare 显示「黄色云朵」（DNS only / 代理）或「橙色云朵」（代理）按邮箱要求设置即可。

---

## 生成好的文件清单

```
G:\来发信开发客户\landing-page\
├── index.html      ← 官网落地页（英文，B2B 毛织工厂定位）
├── 404.html        ← 404 错误页
└── images\
    ├── logo.png                    ← 品牌 logo
    ├── hero.jpg                    ← 首页主图（模特开衫）
    ├── product-polo-green.jpg      ← 橄榄绿针织 Polo
    ├── product-polo-khaki.jpg      ← 卡其针织 Polo
    ├── product-dress-wine.jpg      ← 酒红针织连衣裙
    └── product-dress-orange.jpg    ← 珊瑚条纹针织背心
```
